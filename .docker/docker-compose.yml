version: '3'

services:
  web:
    build: 
        context: .
        dockerfile: './nginx/Dockerfile'
    ports:
        - "80:80"
  php:
    build: 
        context: .
        dockerfile: './php-fpm/Dockerfile'
